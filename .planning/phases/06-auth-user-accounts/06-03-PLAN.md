---
phase: 06-auth-user-accounts
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [src/lib/firebase/firestore.ts, src/store/use-profile-store.ts, src/hooks/use-profile-sync.ts]
autonomous: true
---

<objective>
Implement cloud synchronization for user profiles and migrate existing LocalStorage data.
Ensures users don't lose their data when logging in for the first time.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-auth-user-accounts/06-01-SUMMARY.md
@src/store/use-profile-store.ts
@src/lib/firebase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Firestore Profile Service</name>
  <files>src/lib/firebase/firestore.ts</files>
  <action>
    Create a utility service to:
    - Save user profile to `/users/{uid}/profile`.
    - Fetch user profile from Firestore.
    - Handle Firestore permission errors.
  </action>
  <verify>Functions for getProfile and saveProfile are exported and typed</verify>
  <done>Backend data persistence layer is ready</done>
</task>

<task type="auto">
  <name>Task 2: Profile Sync Hook & Migration Logic</name>
  <files>src/hooks/use-profile-sync.ts</files>
  <action>
    Create a `useProfileSync` hook that:
    - Monitors auth state.
    - When user logs in:
      1. Check if Firestore has a profile.
      2. If not, and LocalStorage HAS a profile, "migrate" it to Firestore.
      3. Update Zustand store with the cloud profile.
    - When profile changes in store, sync to Firestore if authenticated.
  </action>
  <verify>Hook correctly triggers migration on login (can be tested with console logs)</verify>
  <done>Local data is seamlessly transitioned to the cloud</done>
</task>

<task type="auto">
  <name>Task 3: Update Zustand Store for Cloud-First</name>
  <files>src/store/use-profile-store.ts</files>
  <action>
    Modify `useProfileStore` to:
    - Support an `isSyncing` state.
    - Add a `setProfileRemote` action that updates state without triggering a sync back to Firestore (to avoid loops).
    - Ensure hydration-safety during the transition from local-only to cloud-synced.
  </action>
  <verify>Store correctly holds and updates profile data</verify>
  <done>Store is optimized for cloud synchronization</done>
</task>

</tasks>

<verification>
- Log in with existing local data - verify it appears in Firestore.
- Modify profile on one device - verify it updates in Firestore.
- Log in on a second device - verify the profile is fetched from Firestore.
</verification>

<success_criteria>
- Seamless LocalStorage to Firestore migration.
- Real-time (or near real-time) profile persistence.
- No data loss during the auth transition.
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-user-accounts/06-03-SUMMARY.md`
</output>
