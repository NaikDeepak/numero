---
phase: 06-auth-user-accounts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/auth/firebase-admin.ts, src/middleware.ts, src/app/api/auth/login/route.ts, src/app/api/auth/logout/route.ts, src/auth/config.ts]
autonomous: true
user_setup:
  - service: firebase
    why: "Server-side authentication and session management"
    env_vars:
      - name: FIREBASE_PROJECT_ID
        source: "Firebase Console -> Project Settings"
      - name: FIREBASE_CLIENT_EMAIL
        source: "Firebase Console -> Project Settings -> Service Accounts"
      - name: FIREBASE_PRIVATE_KEY
        source: "Firebase Console -> Project Settings -> Service Accounts -> Generate new private key"
      - name: AUTH_COOKIE_NAME
        source: "Static string (e.g., __session)"
      - name: AUTH_COOKIE_SIGNATURE_KEY_1
        source: "Random 32+ char string"
      - name: AUTH_COOKIE_SIGNATURE_KEY_2
        source: "Random 32+ char string"

must_haves:
  truths:
    - "Session cookies are automatically managed by middleware"
    - "Server components can access user tokens via cookies"
    - "Protected routes redirect to login if no session exists"
  artifacts:
    - path: "src/auth/firebase-admin.ts"
      provides: "Server-side Firebase Admin SDK"
    - path: "src/middleware.ts"
      provides: "Edge-runtime session management"
    - path: "src/app/api/auth/login/route.ts"
      provides: "ID token to Session Cookie exchange"
  key_links:
    - from: "middleware.ts"
      to: "next-firebase-auth-edge"
      via: "authMiddleware wrapper"
---

<objective>
Setup the server-side authentication foundation using next-firebase-auth-edge.
This enables SSR-compatible authentication and secure route protection at the edge.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-auth-user-accounts/06-RESEARCH.md
@src/lib/firebase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Dependencies</name>
  <files>package.json</files>
  <action>
    Install required packages:
    - `next-firebase-auth-edge`
    - `firebase-admin`
  </action>
  <verify>npm list next-firebase-auth-edge firebase-admin</verify>
  <done>Packages installed and added to package.json</done>
</task>

<task type="auto">
  <name>Task 2: Auth Configuration and Admin SDK</name>
  <files>src/auth/config.ts, src/auth/firebase-admin.ts</files>
  <action>
    - Create `src/auth/config.ts` containing the auth configuration (cookie settings, API keys).
    - Create `src/auth/firebase-admin.ts` to initialize the Firebase Admin SDK using service account environment variables. Ensure private key newlines are handled correctly.
  </action>
  <verify>Files exist and export required constants/instances</verify>
  <done>Admin SDK and Auth configuration are ready for use</done>
</task>

<task type="auto">
  <name>Task 3: Implement Auth Middleware</name>
  <files>src/middleware.ts</files>
  <action>
    Implement Next.js middleware using `authMiddleware` from `next-firebase-auth-edge`.
    - Configure it to handle session cookies.
    - Define public vs protected routes (e.g., /dashboard, /profile should be protected).
    - Handle redirects for unauthenticated users.
  </action>
  <verify>Middleware correctly intercepts requests and manages cookies (can be tested via curl or browser in next plan)</verify>
  <done>Edge middleware is protecting routes and managing session state</done>
</task>

<task type="auto">
  <name>Task 4: Create Session API Routes</name>
  <files>src/app/api/auth/login/route.ts, src/app/api/auth/logout/route.ts</files>
  <action>
    Implement API endpoints for login and logout.
    - `login`: Accepts ID Token from client, validates it, and sets session cookie.
    - `logout`: Clears the session cookie.
    Use helpers from `next-firebase-auth-edge`.
  </action>
  <verify>POST /api/auth/login and POST /api/auth/logout respond with appropriate Set-Cookie headers</verify>
  <done>Server-side session management endpoints are functional</done>
</task>

</tasks>

<verification>
- Verify middleware redirects /profile to /login.
- Verify /api/auth/login returns a session cookie when provided a valid ID token (mocked if necessary).
</verification>

<success_criteria>
- Firebase Admin SDK initialized successfully.
- Middleware handles session cookies at the Edge.
- API routes for login/logout are functional.
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-user-accounts/06-01-SUMMARY.md`
</output>
