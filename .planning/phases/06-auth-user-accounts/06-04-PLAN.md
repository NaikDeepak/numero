---
phase: 06-auth-user-accounts
plan: 04
type: auto
wave: 1
depends_on: [06-03]
autonomous: true
must_haves:
  truths:
    - "Premium users have 'premium' custom claim"
    - "Middleware blocks non-premium users from /reports/premium"
    - "Server actions can verify custom claims"
  artifacts:
    - path: "src/app/actions/admin.ts"
      provides: "Role management actions"
    - path: "src/app/upgrade/page.tsx"
      provides: "Upgrade UI"
    - path: "src/middleware.ts"
      provides: "Route protection"
---

<objective>
Implement Premium Tier infrastructure using Firebase Custom Claims. creating a secure way to gate content and manageable roles (User vs Premium vs Admin).
</objective>

<context>
@.planning/phases/06-auth-user-accounts/06-RESEARCH.md
@src/middleware.ts
@src/auth/config.ts
@src/auth/get-auth-user.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Role Management Logic</name>
  <files>src/lib/auth/roles.ts, src/app/actions/admin.ts</files>
  <action>
    1. Create `src/lib/auth/roles.ts`:
       - Function `setCustomUserClaims(uid: string, claims: object)` using firebase-admin.
       - Function `getUserClaims(uid: string)`.
    2. Create `src/app/actions/admin.ts`:
       - Server Action `togglePremiumStatus()` (Self-service for MVP/Dev, or Admin-only later).
       - Ensure it verifies the caller is authenticated.
       - Use `revalidatePath` to refresh session.
  </action>
  <verify>
    Create a test script `scripts/test-claims.ts` or use existing test runner to verify claims can be set and retrieved.
  </verify>
</task>

<task type="auto">
  <name>Task 2: Protected Routes & Middleware</name>
  <files>src/middleware.ts, src/app/reports/premium/page.tsx, src/app/upgrade/page.tsx</files>
  <action>
    1. Create `src/app/reports/premium/page.tsx`:
       - A dummy premium content page.
       - Server component that checks `getAuthUser()` for `isPremium`.
    2. Create `src/app/upgrade/page.tsx`:
       - Simple marketing page with a button to trigger `togglePremiumStatus` (simulating purchase).
    3. Verify `src/middleware.ts`:
       - Ensure `/reports/premium` is gated.
       - Ensure non-premium users are redirected to `/upgrade`.
  </action>
  <verify>
    Manual check or e2e test:
    1. As Guest -> Access /reports/premium -> Redirect to Login.
    2. As Free User -> Access /reports/premium -> Redirect to Upgrade.
    3. Click Upgrade -> Become Premium -> Access /reports/premium -> Success.
  </verify>
</task>

<task type="auto">
  <name>Task 3: UI Integration</name>
  <files>src/components/layout/header.tsx, src/components/auth/user-dropdown.tsx</files>
  <action>
    1. Update `UserDropdown` to show a "Premium" badge if the user has the claim.
    2. Add "Upgrade" link in dropdown if user is free tier.
  </action>
</task>
</tasks>

<success_criteria>
- [ ] User can "upgrade" (toggle premium) via server action.
- [ ] Middleware correctly gates `/reports/premium`.
- [ ] UI reflects premium status immediately after upgrade.
</success_criteria>

<output>
Create .planning/phases/06-auth-user-accounts/06-04-SUMMARY.md
</output>
