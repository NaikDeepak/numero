---
phase: 06-auth-user-accounts
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [src/auth/auth-provider.tsx, src/components/auth/login-form.tsx, src/components/auth/signup-form.tsx, src/app/layout.tsx, src/app/(auth)/login/page.tsx, src/components/premium-gate.tsx]
autonomous: false

must_haves:
  truths:
    - "Login and Signup forms are visible at /login"
    - "User can authenticate via Firebase SDK"
    - "Auth state is synchronized to the server via session cookies"
    - "UI components react to premium status (e.g., badges, restricted buttons)"
  artifacts:
    - path: "src/auth/auth-provider.tsx"
      provides: "React Context for Auth state"
    - path: "src/components/auth/login-form.tsx"
      provides: "Login UI with cookie sync"
    - path: "src/components/premium-gate.tsx"
      provides: "Declarative UI gating component"
  key_links:
    - from: "src/components/auth/login-form.tsx"
      to: "/api/auth/login"
      via: "fetch POST with ID token"
      pattern: "fetch.*api/auth/login"
---

<objective>
Implement the client-side authentication UI and state management, including premium tier visibility logic.
Provides users with the ability to sign in/up and access their profile and premium features.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-auth-user-accounts/06-01-SUMMARY.md
@src/auth/config.ts
@src/lib/firebase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth Provider & Hook</name>
  <files>src/auth/auth-provider.tsx</files>
  <action>
    Create a React 19 compatible `AuthProvider` that:
    - Receives initial user state from Server Components.
    - Provides `user` and `loading` state to the client tree.
    - Exposes custom claims (including `premium` status) from the decoded token.
    - Synchronizes with Firebase client SDK `onIdTokenChanged` to handle token refreshes.
  </action>
  <verify>AuthProvider exports useAuth and wraps children</verify>
  <done>Global auth state and claims are accessible to all client components</done>
</task>

<task type="auto">
  <name>Task 2: Login and Signup Forms with Cookie Sync</name>
  <files>src/components/auth/login-form.tsx, src/components/auth/signup-form.tsx</files>
  <action>
    Build the authentication forms using:
    - Tailwind v4 for styling.
    - `react-hook-form` + `zod` for validation.
    - `signInWithEmailAndPassword` and `createUserWithEmailAndPassword` from Firebase SDK.
    - IMPORTANT: Call `/api/auth/login` after successful Firebase auth to sync session cookie.
  </action>
  <verify>Forms render correctly and handle validation errors; /api/auth/login is called on success</verify>
  <done>Users can enter credentials and trigger authentication with session persistence</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Auth in Layout & Navigation</name>
  <files>src/app/layout.tsx, src/components/nav-main.tsx</files>
  <action>
    - Wrap the root layout with `AuthProvider`.
    - Update navigation to show user profile/logout when authenticated, and Login when not.
    - Use Server Components to pass initial session state to the Provider to avoid hydration flashes.
  </action>
  <verify>Navigation changes state based on login status</verify>
  <done>Authentication is visually integrated into the application</done>
</task>

<task type="auto">
  <name>Task 4: Premium Feature Gating (UI)</name>
  <files>src/components/premium-gate.tsx</files>
  <action>
    Create a `PremiumGate` component and utility hooks to:
    - Check the `premium` claim from `useAuth`.
    - Conditionally render children or a "locked" placeholder/upgrade prompt.
    - Apply this to sensitive UI elements (e.g., "Advanced Compatibility Report" button).
  </action>
  <verify>PremiumGate correctly shows/hides content based on user claims</verify>
  <done>Client-side premium feature gating is enforced in the UI</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 5: Verify Auth and Premium Flow</name>
  <action>Verify the full login, logout, and premium gating flow</action>
  <what-built>Authentication UI, session management, and premium gating</what-built>
  <how-to-verify>
    1. Go to /login.
    2. Create a new account.
    3. Verify redirect to home/profile.
    4. Verify premium features are "locked" for new users.
    5. (Optional/Mock) Assign 'premium' claim to user and verify features unlock.
    6. Log out - verify redirect and session cleared.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- User can create an account and log in.
- Session persists across page refreshes.
- UI elements react correctly to premium vs free status.
- Logout clears session and redirects.
</verification>

<success_criteria>
- Functional login/signup UI.
- Hydration-safe auth state.
- Successful premium gating in UI components.
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-user-accounts/06-02-SUMMARY.md`
</output>
