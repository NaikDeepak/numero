---
phase: 06-auth-user-accounts
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [src/auth/auth-provider.tsx, src/components/auth/login-form.tsx, src/components/auth/signup-form.tsx, src/app/layout.tsx, src/app/(auth)/login/page.tsx]
autonomous: false
---

<objective>
Implement the client-side authentication UI and state management.
Provides users with the ability to sign in/up and access their profile.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-auth-user-accounts/06-01-SUMMARY.md
@src/auth/config.ts
@src/lib/firebase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth Provider & Hook</name>
  <files>src/auth/auth-provider.tsx</files>
  <action>
    Create a React 19 compatible `AuthProvider` that:
    - Receives initial user state from Server Components.
    - Provides `user` and `loading` state to the client tree.
    - Synchronizes with Firebase client SDK `onIdTokenChanged` to handle token refreshes.
  </action>
  <verify>AuthProvider exports useAuth and wraps children</verify>
  <done>Global auth state is accessible to all client components</done>
</task>

<task type="auto">
  <name>Task 2: Login and Signup Forms</name>
  <files>src/components/auth/login-form.tsx, src/components/auth/signup-form.tsx</files>
  <action>
    Build the authentication forms using:
    - Tailwind v4 for styling (consistent with current design system).
    - `react-hook-form` + `zod` for validation.
    - `signInWithEmailAndPassword` and `createUserWithEmailAndPassword` from Firebase SDK.
    - Call `/api/auth/login` after successful Firebase auth to sync session cookie.
  </action>
  <verify>Forms render correctly and handle validation errors</verify>
  <done>Users can enter credentials and trigger authentication</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Auth in Layout & Navigation</name>
  <files>src/app/layout.tsx, src/components/nav-main.tsx</files>
  <action>
    - Wrap the root layout with `AuthProvider`.
    - Update navigation to show user profile/logout when authenticated, and Login when not.
    - Use Server Components to pass initial session state to the Provider to avoid hydration flashes.
  </action>
  <verify>Navigation changes state based on login status</verify>
  <done>Authentication is visually integrated into the application</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: Verify Auth Flow</name>
  <action>Verify the full login and logout flow</action>
  <what-built>Authentication UI and session management</what-built>
  <how-to-verify>
    1. Go to /login.
    2. Create a new account.
    3. Verify redirect to home/profile.
    4. Refresh page - verify you are still logged in.
    5. Log out - verify redirect and session cleared.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- User can create an account and log in.
- Session persists across page refreshes.
- Logout clears session and redirects.
</verification>

<success_criteria>
- Functional login/signup UI.
- Hydration-safe auth state.
- Successful round-trip from Client SDK -> API Route -> Middleware.
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-user-accounts/06-02-SUMMARY.md`
</output>
