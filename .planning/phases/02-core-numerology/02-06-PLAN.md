---
phase: 02-core-numerology
plan: 06
type: execute
wave: 1
depends_on: []
files_modified: [src/components/ui/form.tsx]
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "App loads without module not found errors"
    - "Form component exists in components/ui"
  artifacts:
    - path: "src/components/ui/form.tsx"
      provides: "Form UI components"
      exports: ["Form", "FormField", "FormItem", "FormLabel", "FormControl", "FormMessage"]
  key_links:
    - from: "src/components/numerology/input-form.tsx"
      to: "src/components/ui/form.tsx"
      via: "import"
---

<objective>
Fix missing dependency for Input Form component.

Purpose: Resolve UAT blocker where app crashes due to missing 'components/ui/form' module.
Output: src/components/ui/form.tsx
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepaknaik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-numerology/02-UAT.md
@src/components/numerology/input-form.tsx
@components.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Form component</name>
  <files>src/components/ui/form.tsx</files>
  <action>
    Install the missing form component using shadcn CLI:

    ```bash
    npx shadcn@latest add form -y
    ```

    If the command fails or hangs, create `src/components/ui/form.tsx` manually with the standard shadcn implementation (wrapping react-hook-form fields with accessible labels and error messages).

    Ensure it exports:
    - Form
    - FormItem
    - FormLabel
    - FormControl
    - FormDescription
    - FormMessage
    - FormField
  </action>
  <verify>
    ls src/components/ui/form.tsx
  </verify>
  <done>File exists and exports required components</done>
</task>

</tasks>

<verification>
Check that the application builds/starts without "Module not found" error for the form component.
</verification>

<success_criteria>
- src/components/ui/form.tsx exists
- Input form component can resolve the import
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-numerology/02-06-SUMMARY.md`
</output>
