---
phase: 02-core-numerology
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/components/numerology/input-form.tsx
  - src/components/numerology/hero-result.tsx
  - src/app/page.tsx
autonomous: false
must_haves:
  truths:
    - "User can enter name and DOB and see immediate validation feedback"
    - "Submitting the form triggers calculation and displays Moolank and Bhagyank badges"
    - "Hero cards animate into view using Framer Motion"
  artifacts:
    - path: "src/components/numerology/input-form.tsx"
      provides: "Zod-validated form for user details"
    - path: "src/components/numerology/hero-result.tsx"
      provides: "Hero badges for primary numbers"
  key_links:
    - from: "src/components/numerology/input-form.tsx"
      to: "src/store/use-profile-store.ts"
      via: "store updates on submit"
    - from: "src/app/page.tsx"
      to: "src/lib/numerology/engine.ts"
      via: "calculating results from store data"
---

<objective>
Implement the user interface for entering details and displaying the primary numerology numbers (Moolank and Bhagyank).

Purpose: Create the primary interaction point for the user and provide immediate value through "Hero" results.
Output: Validated input form and animated hero cards for Moolank and Bhagyank.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepaknaik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/numerology/types.ts
@src/store/use-profile-store.ts
</context>

<tasks>

<task type="auto">
  <name>Implement Numerology Input Form</name>
  <files>src/components/numerology/input-form.tsx</files>
  <action>
    Create a form using shadcn/ui (Form, Input, Select, Button) and react-hook-form.
    - Validate with `NumerologyInputSchema` from `types.ts`.
    - Input for Full Name.
    - Masked input or date field for DOB (DD/MM/YYYY).
    - Select for Gender (Male, Female, Other).
    - On submit: Save to `use-profile-store`.
  </action>
  <verify>Try submitting with invalid date and verify error message.</verify>
  <done>Form is functional and updates the global store.</done>
</task>

<task type="auto">
  <name>Create Hero Result Badges</name>
  <files>src/components/numerology/hero-result.tsx</files>
  <action>
    Create large, visually striking "Hero" badges for Moolank and Bhagyank.
    - Use Framer Motion for a "scale-up" or "fade-in" entrance animation.
    - Display the number and its name (e.g., "Moolank: 7").
    - Use modern minimal styling (clean borders, subtle shadows).
  </action>
  <verify>Check that badges animate into view when the component mounts.</verify>
  <done>Hero results are visually impressive and animated.</done>
</task>

<task type="auto">
  <name>Wire Main Page Flow</name>
  <files>src/app/page.tsx</files>
  <action>
    Orchestrate the landing page experience.
    - If no profile in store: Show `InputForm`.
    - If profile exists: Calculate results using `engine.ts` and show `HeroResult`.
    - Add a "Reset" button to clear profile and return to form.
    - Use AnimatePresence for smooth transitions between form and results.
  </action>
  <verify>Submit form and check that results replace the form smoothly.</verify>
  <done>The core app flow (Input -> Results) is complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Input form and hero result badges</what-built>
  <how-to-verify>
    1. Open the app and verify the input form layout.
    2. Enter details and click submit.
    3. Verify Moolank and Bhagyank badges appear with an animation.
    4. Refresh the page and ensure the results remain visible.
    5. Click reset and verify the form returns.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
User can successfully navigate from input to seeing their core numbers.
</verification>

<success_criteria>
Form correctly validates, store persists, and hero results animate into view.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-numerology/02-03-SUMMARY.md`
</output>
