---
phase: 02-core-numerology
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/numerology/types.ts
  - src/lib/numerology/engine.ts
  - src/lib/numerology/engine.test.ts
autonomous: true
must_haves:
  truths:
    - "Core calculation engine produces correct Moolank and Bhagyank numbers"
    - "Grid numbers correctly follow legacy exclusion rules for day 10, 20, 30"
    - "Name numerology correctly handles master numbers 11 and 22"
  artifacts:
    - path: "src/lib/numerology/types.ts"
      provides: "Zod schemas for numerology inputs and results"
    - path: "src/lib/numerology/engine.ts"
      provides: "Stateless calculation functions"
  key_links:
    - from: "src/lib/numerology/engine.ts"
      to: "src/lib/numerology/types.ts"
      via: "Type imports and Zod parsing"
---

<objective>
Port the proven numerology calculation logic from the legacy codebase to a type-safe, stateless engine.

Purpose: Ensure accuracy of the core "brain" while enabling modern TypeScript development.
Output: Validated calculation engine and comprehensive test suite.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepaknaik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/utils/numerologyUtils.js
</context>

<tasks>

<task type="auto">
  <name>Define Numerology Schemas</name>
  <files>src/lib/numerology/types.ts</files>
  <action>
    Create Zod schemas for all numerology entities.
    - NumerologyInput: name (string), dob (string/date), gender (enum: Male, Female, Other).
    - NumerologyResult: moolank (number), bhagyank (number), kua (number/string), gridNumbers (number[]).
    - NameNumbers: destiny, soulUrge, personality.
    Use export type z.infer<typeof ...> for TypeScript types.
  </action>
  <verify>Run a script to parse valid/invalid objects against these schemas.</verify>
  <done>Types and schemas are defined and exported.</done>
</task>

<task type="auto">
  <name>Implement Ported Logic</name>
  <files>src/lib/numerology/engine.ts</files>
  <action>
    Implement calculation functions based on api/utils/numerologyUtils.js.
    - reduceToSingleDigit(num): Standard reduction.
    - reduceToSingleDigitOrMaster(num): Reduction keeping 11/22.
    - calculateNumerologyData(dob, gender): Core logic including Kua and Grid.
    - calculateNameNumbers(name): Pythagorean map logic.
    - calculatePersonalYear/Month/Day.
    MANDATORY: Preserve the logic in api/utils/numerologyUtils.js:96 regarding Moolank grid inclusion.
  </action>
  <verify>Check that functions are pure and return expected results for known inputs.</verify>
  <done>All legacy logic is ported and type-safe.</done>
</task>

<task type="auto">
  <name>Create Engine Tests</name>
  <files>src/lib/numerology/engine.test.ts</files>
  <action>
    Write unit tests for all engine functions using Vitest/Jest.
    - Test DOB 1980-01-01 (Moolank 1, Bhagyank 1).
    - Test DOB with day 10, 20, 30 to verify Moolank exclusion from grid.
    - Test name "Claude" to verify Pythagorean values.
    - Test names that result in 11 or 22 to verify master number preservation.
  </action>
  <verify>Run npm test src/lib/numerology/engine.test.ts</verify>
  <done>All tests pass with 100% coverage of calculation logic.</done>
</task>

</tasks>

<verification>
Verify that calculation outputs exactly match legacy outputs for the same inputs.
</verification>

<success_criteria>
Stateless engine is fully functional and verified via automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-numerology/02-01-SUMMARY.md`
</output>
