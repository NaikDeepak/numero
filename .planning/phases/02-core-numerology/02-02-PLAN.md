---
phase: 02-core-numerology
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/store/use-profile-store.ts
  - src/lib/numerology/data-loader.ts
autonomous: true
must_haves:
  truths:
    - "User profile data persists across page reloads"
    - "Interpretation data is correctly loaded and validated from JSON files"
    - "Store handles hydration to avoid SSR mismatches"
  artifacts:
    - path: "src/store/use-profile-store.ts"
      provides: "Persistent Zustand store for user profiles"
    - path: "src/lib/numerology/data-loader.ts"
      provides: "Utility to load and validate interpretation data"
  key_links:
    - from: "src/store/use-profile-store.ts"
      to: "localStorage"
      via: "Zustand persist middleware"
---

<objective>
Setup the persistence layer for user profiles and the data loading mechanism for numerology interpretations.

Purpose: Provide a reliable state management system that survives browser refreshes and a type-safe way to access mystical data.
Output: A persistent Zustand store and a data loading utility.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepaknaik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/data/moolankMeanings.json
@api/data/gridAnalysisDefinitions.json
</context>

<tasks>

<task type="auto">
  <name>Create Interpretation Data Loader</name>
  <files>src/lib/numerology/data-loader.ts</files>
  <action>
    Implement a utility to fetch and validate interpretation JSON files.
    - Define Zod schemas for `moolankMeanings.json` and `gridAnalysisDefinitions.json`.
    - Create a `loadMeanings` function that reads from the `api/data` directory (or a copy in `public/data` if needed for client-side fetch).
    - Implement lookup helpers (e.g., `getMoolankMeaning(number)`).
  </action>
  <verify>Call the loader in a test script and assert that valid data is returned for number 1.</verify>
  <done>Interpretation data is accessible and type-safe.</done>
</task>

<task type="auto">
  <name>Implement Persistent Profile Store</name>
  <files>src/store/use-profile-store.ts</files>
  <action>
    Create a Zustand store using the `persist` middleware.
    - State: `profile` (name, dob, gender), `isHydrated` (boolean).
    - Actions: `setProfile(data)`, `clearProfile()`.
    - Implement an `onRehydrateStorage` hook to set `isHydrated` to true.
    - Use the "Mounted State" pattern to prevent SSR hydration mismatches in components.
  </action>
  <verify>Set a profile in the console, refresh the page, and verify the state is recovered.</verify>
  <done>User profile data is persisted in localStorage and accessible via a reactive store.</done>
</task>

</tasks>

<verification>
Ensure that data modified in one session is present when the browser is reopened.
</verification>

<success_criteria>
Store persists correctly and interpretation data is validated against Zod schemas.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-numerology/02-02-SUMMARY.md`
</output>
