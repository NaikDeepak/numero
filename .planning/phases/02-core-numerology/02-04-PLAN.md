---
phase: 02-core-numerology
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - src/components/numerology/lo-shu-grid.tsx
  - src/lib/numerology/grid-utils.ts
autonomous: true
must_haves:
  truths:
    - "Numbers from DOB and calculated results are correctly placed in the 3x3 grid"
    - "Grid cells use modern minimal elemental colors (Water, Earth, Wood, Metal, Fire)"
    - "Numbers in the grid reveal themselves with a staggered animation"
    - "Grid patterns (arrows/planes) are correctly identified and listed"
  artifacts:
    - path: "src/components/numerology/lo-shu-grid.tsx"
      provides: "Visual 3x3 Lo Shu Grid component"
    - path: "src/lib/numerology/grid-utils.ts"
      provides: "Pattern analysis logic for arrows and planes"
  key_links:
    - from: "src/components/numerology/lo-shu-grid.tsx"
      to: "Framer Motion"
      via: "staggerChildren animation variants"
    - from: "src/components/numerology/grid-utils.ts"
      to: "api/data/gridAnalysisDefinitions.json"
      via: "pattern matching against definitions"
---

<objective>
Implement the visual representation of the Lo Shu Grid and the logic to identify patterns (arrows/planes) within the grid.

Purpose: Provide a visual anchor for the numerology report and surface deeper patterns beyond single numbers.
Output: Animated 3x3 Lo Shu Grid and pattern analysis list.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepaknaik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/utils/numerologyUtils.js
@api/data/gridAnalysisDefinitions.json
</context>

<tasks>

<task type="auto">
  <name>Implement Grid Pattern Analysis</name>
  <files>src/lib/numerology/grid-utils.ts</files>
  <action>
    Port and enhance the `analyzeGrid` logic from `api/utils/numerologyUtils.js`.
    - Implement functions to detect full rows, columns, and diagonals (Arrows of Strength).
    - Implement functions to detect empty rows, columns, and diagonals (Arrows of Weakness).
    - Map these patterns to the names and interpretations in `gridAnalysisDefinitions.json`.
    - Ensure it handles the legacy "gridNumbers" array correctly.
  </action>
  <verify>Run analysis on a known grid (e.g., 1-2-3 present) and verify "Arrow of Willpower" is detected.</verify>
  <done>Logic correctly identifies grid patterns based on definitions.</done>
</task>

<task type="auto">
  <name>Create Lo Shu Grid Component</name>
  <files>src/components/numerology/lo-shu-grid.tsx</files>
  <action>
    Build a 3x3 grid component using Tailwind CSS.
    - Standard Lo Shu positions:
      Top: 4, 9, 2
      Mid: 3, 5, 7
      Bottom: 8, 1, 6
    - Apply elemental colors:
      Water (1): bg-slate-900/10 text-slate-900
      Earth (2, 5, 8): bg-stone-200/50 text-stone-800
      Wood (3, 4): bg-emerald-100/50 text-emerald-900
      Metal (6, 7): bg-zinc-100/80 text-zinc-900
      Fire (9): bg-rose-100/50 text-rose-900
    - Use Framer Motion for staggered reveal of numbers in each cell.
  </action>
  <verify>Mount the component with sample numbers and check layout/colors/animation.</verify>
  <done>Grid is visually correct and animates numbers into their respective cells.</done>
</task>

<task type="auto">
  <name>Integrate Grid into Results Display</name>
  <files>src/components/numerology/hero-result.tsx</files>
  <action>
    Update the results view to include the grid and its analysis.
    - Render `LoShuGrid` below the hero badges.
    - Display a list of identified "Arrows" with their names and brief interpretations.
    - Ensure responsive layout (grid should scale down on small screens).
  </action>
  <verify>Submit the form and verify the grid appears with correct numbers and patterns.</verify>
  <done>Grid and pattern analysis are integrated into the main user flow.</done>
</task>

</tasks>

<verification>
Grid correctly reflects the DOB/results and identifies patterns like "Arrow of Intellect" (3-6-9).
</verification>

<success_criteria>
Grid displays correctly, colors match elemental spec, and patterns are accurately detected.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-numerology/02-04-SUMMARY.md`
</output>
