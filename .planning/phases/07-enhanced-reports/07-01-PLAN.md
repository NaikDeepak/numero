---
phase: 07-enhanced-reports
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [src/lib/numerology/remedies.ts, src/lib/numerology/compatibility-logic.ts]
autonomous: true

must_haves:
  truths:
    - "Missing numbers are correctly identified from grid results"
    - "Remedies are correctly mapped from JSON data for each missing number"
    - "Compatibility score is deterministic based on Moolank/Bhagyank matching"
  artifacts:
    - path: "src/lib/numerology/remedies.ts"
      provides: "Missing number and remedy lookup logic"
    - path: "src/lib/numerology/compatibility-logic.ts"
      provides: "Deterministic scoring engine"
  key_links:
    - from: "src/lib/numerology/remedies.ts"
      to: "api/data/missingNumberRemedies.json"
      via: "import and lookup"
---

<objective>
Implement the deterministic logic for identifying missing numbers, fetching their remedies from the existing JSON dataset, and calculating a consistent compatibility score.

Purpose: Ensure accuracy in the report before building the UI.
Output: Tested logic files for remedies and scoring.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-enhanced-reports/RESEARCH.md
@src/lib/numerology/engine.ts
@api/data/missingNumberRemedies.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Missing Number & Remedy Logic</name>
  <files>src/lib/numerology/remedies.ts</files>
  <action>
    Create `src/lib/numerology/remedies.ts`.
    Implement `getMissingNumbers(gridNumbers: number[]): number[]`.
    Implement `getRemediesForNumbers(numbers: number[]): Remedy[]`.
    Map numbers 1-9 against the input grid. Use the data in `api/data/missingNumberRemedies.json` for mapping.
  </action>
  <verify>Run `vitest` with a new test file `src/lib/numerology/remedies.test.ts` verifying missing number detection for various DOBs.</verify>
  <done>Logic correctly identifies 100% of missing numbers and returns correct remedy strings.</done>
</task>

<task type="auto">
  <name>Task 2: Deterministic Compatibility Scoring</name>
  <files>src/lib/numerology/compatibility-logic.ts</files>
  <action>
    Create `src/lib/numerology/compatibility-logic.ts`.
    Implement `calculateCompatibilityScore(user: NumerologyResult, partner: NumerologyResult): number`.
    Base the score on:
    - Moolank match (High weight)
    - Bhagyank match (High weight)
    - Grid completion synergy (Does one have what the other lacks?)
    Return a value 0-100.
  </action>
  <verify>Add tests to `src/lib/numerology/compatibility-logic.test.ts` verifying scores for known "perfect" and "clashing" matches.</verify>
  <done>Scoring engine returns consistent results for identical inputs.</done>
</task>

</tasks>

<success_criteria>
All logic tests pass and remedies are correctly mapped from JSON.
</success_criteria>

<output>
After completion, create `.planning/phases/07-enhanced-reports/07-01-SUMMARY.md`
</output>
