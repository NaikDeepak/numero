---
phase: 07-enhanced-reports
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: [src/lib/pdf/compatibility.ts, src/lib/pdf/generator.ts, src/app/api/report/compatibility/route.ts]
autonomous: true

must_haves:
  truths:
    - "PDF report contains side-by-side Lo Shu grids"
    - "PDF report includes the deterministic compatibility score"
    - "PDF report lists the remedies for missing numbers for both profiles"
  artifacts:
    - path: "src/lib/pdf/compatibility.ts"
      provides: "PDF layout logic for dual-profile reports"
  key_links:
    - from: "src/lib/pdf/generator.ts"
      to: "src/lib/pdf/compatibility.ts"
      via: "modular function call"
---

<objective>
Implement the PDF export functionality for the enhanced compatibility report, ensuring all new data (dual grids, score, remedies) is rendered professionally in the generated document.

Purpose: Allow users to save and share their detailed compatibility analysis.
Output: A multi-page, styled PDF report for compatibility matches.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-enhanced-reports/07-02-SUMMARY.md
@src/lib/pdf/generator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dual-Profile PDF Layout</name>
  <files>src/lib/pdf/compatibility.ts</files>
  <action>
    Create `src/lib/pdf/compatibility.ts`.
    Implement functions to render side-by-side Lo Shu grids in `pdfkit`.
    Add sections for:
    - Shared Score & Analysis
    - Individual Grid Breakdowns
    - Missing Number Remedies (categorized by person)
    Maintain the existing "Modern Minimal" styling (fonts, colors).
  </action>
  <verify>Run a test script that generates a `test-compatibility.pdf` and inspect it visually.</verify>
  <done>PDF accurately reflects the information seen in the web UI.</done>
</task>

<task type="auto">
  <name>Task 2: API Integration</name>
  <files>src/app/api/report/compatibility/route.ts</files>
  <action>
    Create or update the API route for compatibility PDF generation.
    It should accept dual profile data, run the logic from 07-01, and stream the PDF from 07-03.
  </action>
  <verify>Test the endpoint with `curl` or Postman to ensure a PDF buffer is returned.</verify>
  <done>API successfully generates a valid PDF from two input profiles.</done>
</task>

<task type="auto">
  <name>Task 3: Wiring Download Button</name>
  <files>src/components/numerology/compatibility-result.tsx</files>
  <action>
    Update the `Download` button in `CompatibilityResult.tsx` to call the new API endpoint.
    Show a loading state (spinner) while the PDF is generating.
  </action>
  <verify>Clicking 'Download' in the UI triggers a browser download of the correct PDF.</verify>
  <done>End-to-end PDF export is working for compatibility reports.</done>
</task>

</tasks>

<success_criteria>
Users can download a high-quality PDF of their compatibility report containing all new data points.
</success_criteria>

<output>
After completion, create `.planning/phases/07-enhanced-reports/07-03-SUMMARY.md`
</output>
