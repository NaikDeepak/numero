---
phase: 07-enhanced-reports
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: [src/components/numerology/compatibility-grid.tsx, src/components/numerology/remedy-section.tsx, src/components/numerology/compatibility-result.tsx]
autonomous: true

must_haves:
  truths:
    - "User can see side-by-side Lo Shu grids for both profiles"
    - "Remedies for missing numbers are displayed in a clear, card-based layout"
    - "The compatibility score is visually represented (e.g., a gauge or progress circle)"
  artifacts:
    - path: "src/components/numerology/compatibility-grid.tsx"
      provides: "Side-by-side or stacked grid comparison"
    - path: "src/components/numerology/remedy-section.tsx"
      provides: "List of remedial measures"
  key_links:
    - from: "src/components/numerology/compatibility-result.tsx"
      to: "src/components/numerology/compatibility-grid.tsx"
      via: "component composition"
---

<objective>
Build the user interface components to visualize the enhanced compatibility data, including the side-by-side Lo Shu grids and the missing number remedies.

Purpose: Provide a rich, data-driven visual report to the user.
Output: Updated CompatibilityResult view with new interactive components.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-enhanced-reports/07-01-SUMMARY.md
@src/components/numerology/lo-shu-grid.tsx
@src/components/numerology/compatibility-result.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Side-by-Side Comparison Grid</name>
  <files>src/components/numerology/compatibility-grid.tsx</files>
  <action>
    Create `src/components/numerology/compatibility-grid.tsx`.
    Implement a component that renders two `LoShuGrid` components.
    Use `flex-col` for mobile and `md:flex-row` for desktop as per research recommendation.
    Add labels identifying "Person A" and "Person B".
  </action>
  <verify>Check visual layout in storybook or local dev server with test data.</verify>
  <done>Grids are clearly visible and responsive on both mobile and desktop views.</done>
</task>

<task type="auto">
  <name>Task 2: Remedy UI Section</name>
  <files>src/components/numerology/remedy-section.tsx</files>
  <action>
    Create `src/components/numerology/remedy-section.tsx`.
    Render a list of cards for missing numbers.
    Each card should show the missing number, its significance, and the specific remedy.
    Use the `remedies.ts` logic from 07-01 to get the data.
  </action>
  <verify>Verify that remedies for missing numbers 2, 4, 8 (example) appear when those numbers are absent from the grid.</verify>
  <done>Remedy cards display correct JSON content and look consistent with the design system.</done>
</task>

<task type="auto">
  <name>Task 3: Update CompatibilityResult</name>
  <files>src/components/numerology/compatibility-result.tsx</files>
  <action>
    Integrate `CompatibilityGrid` and `RemedySection` into the main `CompatibilityResult` component.
    Add a "Compatibility Score" visual (e.g., a semi-circle gauge or styled percentage) using the logic from 07-01.
    Refactor existing cards to fit the new layout.
  </action>
  <verify>Full report renders all sections: Score -> Analysis -> Comparison Grids -> Remedies.</verify>
  <done>The enhanced compatibility report is fully functional in the UI.</done>
</task>

</tasks>

<success_criteria>
The compatibility report UI shows the side-by-side grids, score, and remedies correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/07-enhanced-reports/07-02-SUMMARY.md`
</output>
