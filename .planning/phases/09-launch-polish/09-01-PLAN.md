---
phase: 09-launch-polish
plan: 01
type: auto
wave: 1
depends_on: [08-02]
autonomous: true
must_haves:
  truths:
    - "Application sends correct robots.txt"
    - "Application sends valid sitemap.xml"
    - "404 errors show branded UI"
    - "Runtime errors show branded UI"
    - "App is installable (manifest.json)"
  artifacts:
    - path: "src/app/robots.ts"
    - path: "src/app/sitemap.ts"
    - path: "src/app/manifest.ts"
    - path: "src/app/not-found.tsx"
    - path: "src/app/error.tsx"
---

<objective>
Create standard production artifacts for SEO, Error Handling, and PWA support to ensure a polished user experience and discoverability.
</objective>

<context>
@.planning/phases/09-launch-polish/09-RESEARCH.md
@src/app/layout.tsx
@.env.example
</context>

<tasks>
<task type="auto">
  <name>Task 1: SEO Files</name>
  <files>src/app/robots.ts, src/app/sitemap.ts</files>
  <action>
    1. Create `src/app/robots.ts`: Allow all agents, disallow `/api/` and `/profile`.
    2. Create `src/app/sitemap.ts`: List static routes (`/`, `/login`, `/register`, `/upgrade`).
  </action>
</task>

<task type="auto">
  <name>Task 2: PWA Manifest</name>
  <files>src/app/manifest.ts</files>
  <action>
    Create `src/app/manifest.ts`:
    - Name: "Numero"
    - Short Name: "Numero"
    - Description: "AI-Driven Numerology"
    - Start URL: "/"
    - Display: "standalone"
    - Background Color: "#111827" (matches theme)
    - Theme Color: "#f59e0b" (amber-500)
  </action>
</task>

<task type="auto">
  <name>Task 3: Error Pages</name>
  <files>src/app/not-found.tsx, src/app/error.tsx</files>
  <action>
    1. Create `src/app/not-found.tsx`: "Cosmic Void" theme. "The numbers don't add up here."
    2. Create `src/app/error.tsx`: "Cosmic Alignment Error". Button to `reset()`.
  </action>
</task>

<task type="auto">
  <name>Task 4: Environment Documentation</name>
  <files>.env.example</files>
  <action>
    Update `.env.example` to include `NEXT_PUBLIC_FIREBASE_VAPID_KEY`.
  </action>
</task>
</tasks>

<success_criteria>
- [ ] `robots.txt` and `sitemap.xml` are generated.
- [ ] `manifest.json` is generated.
- [ ] Custom 404 and Error pages exist.
</success_criteria>

<output>
Create .planning/phases/09-launch-polish/09-01-SUMMARY.md
</output>
