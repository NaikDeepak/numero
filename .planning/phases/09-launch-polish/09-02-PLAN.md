---
phase: 09-launch-polish
plan: 02
type: auto
wave: 1
depends_on: [09-01]
autonomous: true
must_haves:
  truths:
    - "Project builds successfully without lint/type errors"
    - "Logging is centralized via a utility"
    - "Temporary files are cleaned up"
  artifacts:
    - path: "src/lib/logger.ts"
      provides: "Structured logging"
---

<objective>
Finalize the codebase for production by implementing a centralized logging strategy, cleaning up development artifacts, and verifying the build integrity.
</objective>

<context>
@.planning/STATE.md
@package.json
</context>

<tasks>
<task type="auto">
  <name>Task 1: Monitoring Infrastructure</name>
  <files>src/lib/logger.ts, src/app/error.tsx</files>
  <action>
    1. Create `src/lib/logger.ts`: A structured wrapper around `console` that can be easily extended for services like Sentry or Datadog later.
    2. Update `src/app/error.tsx` to use this logger.
  </action>
</task>

<task type="auto">
  <name>Task 2: Project Cleanup</name>
  <files>scripts/generate-pdf-test.ts</files>
  <action>
    1. Move `test-pdf.ts` to `scripts/generate-pdf-test.ts`.
    2. Delete `.planning/ROADMAP.md.bak` if it exists.
    3. Update `package.json` to include a script for the PDF test if useful.
  </action>
</task>

<task type="auto">
  <name>Task 3: Final Build Verification</name>
  <files>package.json</files>
  <action>
    1. Run `npm run type-check`.
    2. Run `npm run build`.
    (If these fail, fix the issues).
  </action>
</task>
</tasks>

<success_criteria>
- [ ] `src/lib/logger.ts` exists and is used.
- [ ] Root directory is clean of test scripts.
- [ ] Build completes successfully.
</success_criteria>

<output>
Create .planning/phases/09-launch-polish/09-02-SUMMARY.md
</output>
