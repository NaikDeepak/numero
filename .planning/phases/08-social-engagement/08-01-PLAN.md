---
phase: 08-social-engagement
plan: 01
type: auto
wave: 1
depends_on: []
autonomous: true
must_haves:
  truths:
    - "OG images are generated dynamically for key routes"
    - "Share button uses native API on mobile"
    - "Share button copies link on desktop"
  artifacts:
    - path: "src/app/opengraph-image.tsx"
      provides: "Default social card"
    - path: "src/components/social/share-button.tsx"
      provides: "Sharing UI"
---

<objective>
Implement the "Viral Loop" infrastructure: Dynamic Open Graph images for rich social previews and a smart Share component that leverages native device capabilities.
</objective>

<context>
@.planning/phases/08-social-engagement/08-RESEARCH.md
@src/app/layout.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Share Component</name>
  <files>src/components/social/share-button.tsx</files>
  <action>
    Create `src/components/social/share-button.tsx`.
    - Props: `title`, `text`, `url`.
    - Logic: Check `if (navigator.share)`.
    - UI: Use Lucide `Share2` icon.
    - Fallback: Copy to clipboard with toast notification.
  </action>
</task>

<task type="auto">
  <name>Task 2: Dynamic OG Images (Root)</name>
  <files>src/app/opengraph-image.tsx</files>
  <action>
    Create `src/app/opengraph-image.tsx`.
    - Use `ImageResponse` from `next/og`.
    - Design: Cosmic gradient background, "Numero" logo, tagline.
    - Size: 1200x630.
  </action>
</task>

<task type="auto">
  <name>Task 3: Integration</name>
  <files>src/components/numerology/daily-forecast.tsx, src/components/numerology/compatibility-result.tsx</files>
  <action>
    1. Add `ShareButton` to `DailyForecast` (share the specific day's insight).
    2. Add `ShareButton` to `CompatibilityResult` (share the match score).
  </action>
</task>
</tasks>

<success_criteria>
- [ ] `ShareButton` handles both native share and clipboard fallback.
- [ ] Root route serves a valid image at `/opengraph-image`.
- [ ] UI components include the share action.
</success_criteria>

<output>
Create .planning/phases/08-social-engagement/08-01-SUMMARY.md
</output>
