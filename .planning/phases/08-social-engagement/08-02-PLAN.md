---
phase: 08-social-engagement
plan: 02
type: auto
wave: 1
depends_on: [06-03]
autonomous: true
must_haves:
  truths:
    - "User can grant notification permission"
    - "FCM token is saved to Firestore under /users/{uid}/devices"
    - "Service worker handles background messages"
  artifacts:
    - path: "src/lib/firebase/messaging.ts"
      provides: "FCM client logic"
    - path: "src/components/social/notification-manager.tsx"
      provides: "Permission UI"
    - path: "public/firebase-messaging-sw.js"
      provides: "Background worker"
---

<objective>
Implement Firebase Cloud Messaging (FCM) infrastructure to enable re-engagement via push notifications. This includes the service worker, permission request UI, and token persistence.
</objective>

<context>
@.planning/phases/08-social-engagement/08-RESEARCH.md
@src/lib/firebase.ts
@src/auth/config.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Service Worker & Configuration</name>
  <files>public/firebase-messaging-sw.js, src/lib/firebase/messaging.ts</files>
  <action>
    1. Create `public/firebase-messaging-sw.js`:
       - Initialize Firebase App (standalone worker config).
       - Initialize Messaging.
       - Handle background messages.
    2. Create `src/lib/firebase/messaging.ts`:
       - `requestNotificationPermission()`: Ask user.
       - `onMessageListener()`: Handle foreground messages.
  </action>
</task>

<task type="auto">
  <name>Task 2: Token Management</name>
  <files>src/hooks/use-fcm-token.ts</files>
  <action>
    Create hook `useFcmToken(user)`.
    - If permission granted, `getToken(messaging)`.
    - Save token to Firestore: `users/{uid}/devices/{token}`.
    - Handle token refresh.
  </action>
</task>

<task type="auto">
  <name>Task 3: UI Integration</name>
  <files>src/components/social/notification-manager.tsx, src/app/profile/page.tsx</files>
  <action>
    1. Create `NotificationManager` component:
       - Show "Enable Notifications" button if permission is default.
       - Show status if granted/denied.
    2. Add to Profile page (if exists) or create a section in Layout/Settings.
  </action>
</task>
</tasks>

<success_criteria>
- [ ] Service worker registers successfully.
- [ ] User can click "Enable Notifications" and prompt appears.
- [ ] FCM Token is written to Firestore for authenticated user.
</success_criteria>

<output>
Create .planning/phases/08-social-engagement/08-02-SUMMARY.md
</output>
