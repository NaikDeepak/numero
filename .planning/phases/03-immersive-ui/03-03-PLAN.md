---
id: "03-03"
phase: "03"
title: "Immersive UI: Cosmic Polish"
description: "Refine the visual aesthetic with subtle background animations and improved typography/spacing."
wave: "2"
depends_on: ["03-01", "03-02"]
autonomous: true
files_modified:
  - "src/app/globals.css"
  - "src/components/ui/card.tsx"
verification:
  - "Background has a subtle 'cosmic' gradient or effect"
  - "Cards have refined hover states"
  - "Typography contrast is optimal in both modes"
must_haves:
  - "Use CSS-only background animations where possible to save JS thread"
  - "Ensure accessible contrast ratios"
---

<context>
The "Cosmic" feel comes from depth and subtle motion. We will add a slow-moving gradient background and refine the card styles to feel more "glassy" or elevated in dark mode.
</context>

<tasks>
  <task>
    <step>1</step>
    <description>Add cosmic gradient animation to globals.css.</description>
    <tool>Edit</tool>
    <args>
      <file_path>src/app/globals.css</file_path>
      <old_string>@layer base {
  * {
    @apply border-border outline-ring/50;
  }
  body {
    @apply bg-background text-foreground;
  }
}</old_string>
      <new_string>@layer base {
  * {
    @apply border-border outline-ring/50;
  }
  body {
    @apply bg-background text-foreground antialiased selection:bg-primary/20;
  }
}

@layer utilities {
  .bg-cosmic {
    background: radial-gradient(circle at 50% 0%, oklch(0.2 0.1 260 / 0.1) 0%, transparent 50%),
                radial-gradient(circle at 0% 50%, oklch(0.2 0.1 300 / 0.1) 0%, transparent 50%),
                radial-gradient(circle at 100% 50%, oklch(0.2 0.1 200 / 0.1) 0%, transparent 50%);
  }

  .dark .bg-cosmic {
    background: radial-gradient(circle at 50% 0%, oklch(0.3 0.1 260 / 0.2) 0%, transparent 60%),
                radial-gradient(circle at 0% 50%, oklch(0.3 0.1 300 / 0.15) 0%, transparent 60%),
                radial-gradient(circle at 100% 50%, oklch(0.3 0.1 200 / 0.15) 0%, transparent 60%);
  }
}</new_string>
    </args>
  </task>

  <task>
    <step>2</step>
    <description>Apply cosmic background to main layout.</description>
    <tool>Edit</tool>
    <args>
      <file_path>src/app/page.tsx</file_path>
      <old_string>className="flex min-h-screen flex-col items-center justify-center p-6 md:p-24 bg-background overflow-hidden"</old_string>
      <new_string>className="flex min-h-screen flex-col items-center justify-center p-6 md:p-24 bg-background bg-cosmic overflow-hidden transition-colors duration-500"</new_string>
    </args>
  </task>

  <task>
    <step>3</step>
    <description>Add glassmorphism effect to Cards.</description>
    <tool>Edit</tool>
    <args>
      <file_path>src/components/ui/card.tsx</file_path>
      <old_string>"rounded-xl border bg-card text-card-foreground shadow-sm",</old_string>
      <new_string>"rounded-xl border bg-card/80 backdrop-blur-sm text-card-foreground shadow-sm transition-all hover:shadow-md hover:border-primary/20",</new_string>
    </args>
  </task>
</tasks>
