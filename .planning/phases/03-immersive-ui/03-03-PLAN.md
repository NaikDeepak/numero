---
id: "03-03"
phase: "03"
title: "Immersive UI: Cosmic Polish"
description: "Refine the visual aesthetic by adding the cosmic background to the layout."
wave: "2"
depends_on: ["03-01"]
autonomous: true
files_modified:
  - "src/app/layout.tsx"
verification:
  - "Background has a subtle 'cosmic' gradient globally"
must_haves:
  - "Apply `bg-cosmic` to layout"
---

<context>
The "Cosmic" feel comes from depth and subtle motion. The `bg-cosmic` class is already defined in `globals.css`, we just need to apply it to the global layout.
</context>

<tasks>
  <task>
    <step>1</step>
    <description>Apply cosmic background to global layout.</description>
    <tool>Edit</tool>
    <args>
      <file_path>src/app/layout.tsx</file_path>
      <old_string><body className={inter.className}></old_string>
      <new_string><body className={`${inter.className} bg-background bg-cosmic transition-colors duration-500`}></new_string>
    </args>
    <verify>Check layout body classes</verify>
    <done>Layout updated with cosmic background</done>
  </task>
</tasks>
