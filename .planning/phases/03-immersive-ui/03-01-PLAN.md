---
id: "03-01"
phase: "03"
title: "Immersive UI: Transitions & Theming"
description: "Implement smooth page transitions and a cosmic-themed dark/light mode toggle."
wave: "1"
depends_on: []
autonomous: true
files_modified:
  - "src/app/template.tsx"
  - "src/components/ui/theme-toggle.tsx"
  - "src/app/layout.tsx"
verification:
  - "User can toggle between light and dark modes via a persistent button in the header"
  - "Page content fades in/out smoothly when navigating"
  - "Theme persistence works across reloads"
must_haves:
  - "Use `template.tsx` for transitions"
  - "Place ThemeToggle in `layout.tsx` for global access"
---

<context>
We are upgrading the UI to be "Immersive" and "Cosmic". We will use `template.tsx` for page transitions and implement a slick Theme Toggle in the global layout.
</context>

<tasks>
  <task>
    <step>1</step>
    <description>Create the Theme Toggle component.</description>
    <tool>Write</tool>
    <args>
      <file_path>src/components/ui/theme-toggle.tsx</file_path>
      <content>
"use client"

import * as React from "react"
import { Moon, Sun } from "lucide-react"
import { useTheme } from "next-themes"
import { Button } from "@/components/ui/button"
import { motion } from "framer-motion"

export function ThemeToggle() {
  const { theme, setTheme } = useTheme()
  const [mounted, setMounted] = React.useState(false)

  React.useEffect(() => {
    setMounted(true)
  }, [])

  if (!mounted) {
    return <Button variant="ghost" size="icon" className="opacity-0" />
  }

  return (
    <Button
      variant="ghost"
      size="icon"
      onClick={() => setTheme(theme === "light" ? "dark" : "light")}
      className="relative w-10 h-10 rounded-full hover:bg-muted transition-colors"
      aria-label="Toggle theme"
    >
      <motion.div
        initial={false}
        animate={{
          scale: theme === "light" ? 1 : 0,
          rotate: theme === "light" ? 0 : 90,
          opacity: theme === "light" ? 1 : 0,
        }}
        transition={{ duration: 0.2 }}
        className="absolute inset-0 flex items-center justify-center"
      >
        <Sun className="h-5 w-5" />
      </motion.div>
      <motion.div
        initial={false}
        animate={{
          scale: theme === "dark" ? 1 : 0,
          rotate: theme === "dark" ? 0 : -90,
          opacity: theme === "dark" ? 1 : 0,
        }}
        transition={{ duration: 0.2 }}
        className="absolute inset-0 flex items-center justify-center"
      >
        <Moon className="h-5 w-5" />
      </motion.div>
    </Button>
  )
}
      </content>
    </args>
    <verify>Check if file exists and contains correct imports</verify>
    <done>ThemeToggle component created with motion animations</done>
  </task>

  <task>
    <step>2</step>
    <description>Create the Template file for page transitions.</description>
    <tool>Write</tool>
    <args>
      <file_path>src/app/template.tsx</file_path>
      <content>
"use client"

import { motion } from "framer-motion"

export default function Template({ children }: { children: React.ReactNode }) {
  return (
    <motion.div
      initial={{ opacity: 0, y: 10 }}
      animate={{ opacity: 1, y: 0 }}
      exit={{ opacity: 0, y: -10 }}
      transition={{ ease: "easeInOut", duration: 0.4 }}
    >
      {children}
    </motion.div>
  )
}
      </content>
    </args>
    <verify>Check if file exists and exports default component</verify>
    <done>Template.tsx created with Framer Motion wrapper</done>
  </task>

  <task>
    <step>3</step>
    <description>Import ThemeToggle in layout.tsx</description>
    <tool>Edit</tool>
    <args>
      <file_path>src/app/layout.tsx</file_path>
      <old_string>import { ThemeProvider } from "@/components/theme-provider"</old_string>
      <new_string>import { ThemeProvider } from "@/components/theme-provider"
import { ThemeToggle } from "@/components/ui/theme-toggle"</new_string>
    </args>
    <verify>Check layout.tsx for ThemeToggle import</verify>
    <done>ThemeToggle imported correctly</done>
  </task>

  <task>
    <step>4</step>
    <description>Add ThemeToggle to the global layout inside ThemeProvider.</description>
    <tool>Edit</tool>
    <args>
      <file_path>src/app/layout.tsx</file_path>
      <old_string>          disableTransitionOnChange
        >
          {children}
        </ThemeProvider></old_string>
      <new_string>          disableTransitionOnChange
        >
          <div className="absolute top-4 right-4 z-50">
            <ThemeToggle />
          </div>
          {children}
        </ThemeProvider></new_string>
    </args>
    <verify>Check layout.tsx for ThemeToggle placement</verify>
    <done>ThemeToggle placed in absolute position in layout</done>
  </task>
</tasks>
