---
id: "03-01"
phase: "03"
title: "Immersive UI: Transitions & Theming"
description: "Implement smooth page transitions and a cosmic-themed dark/light mode toggle."
wave: "1"
depends_on: []
autonomous: true
files_modified:
  - "src/app/template.tsx"
  - "src/components/ui/theme-toggle.tsx"
  - "src/app/layout.tsx"
  - "src/app/page.tsx"
verification:
  - "User can toggle between light and dark modes with a smooth icon transition"
  - "Page content fades in/out smoothly when navigating (or mounting)"
  - "Theme persistence works across reloads"
must_haves:
  - "Use `template.tsx` for transitions to ensure they trigger on route changes"
  - "Use `AnimatePresence` for exit animations"
  - "Avoid hydration mismatch in theme toggle"
---

<context>
We are upgrading the UI to be "Immersive" and "Cosmic". The first step is handling the global visual state (Theme) and the transitions between states. We will use `template.tsx` for page transitions as it is the Next.js App Router standard for per-route animation boundaries. We will also implement a slick Theme Toggle that fits the minimal aesthetic.
</context>

<tasks>
  <task>
    <step>1</step>
    <description>Create the Theme Toggle component.</description>
    <tool>Write</tool>
    <args>
      <file_path>src/components/ui/theme-toggle.tsx</file_path>
      <content>
"use client"

import * as React from "react"
import { Moon, Sun } from "lucide-react"
import { useTheme } from "next-themes"
import { Button } from "@/components/ui/button"
import { motion } from "framer-motion"

export function ThemeToggle() {
  const { theme, setTheme } = useTheme()
  const [mounted, setMounted] = React.useState(false)

  React.useEffect(() => {
    setMounted(true)
  }, [])

  if (!mounted) {
    return <Button variant="ghost" size="icon" className="opacity-0" />
  }

  return (
    <Button
      variant="ghost"
      size="icon"
      onClick={() => setTheme(theme === "light" ? "dark" : "light")}
      className="relative w-10 h-10 rounded-full hover:bg-muted transition-colors"
      aria-label="Toggle theme"
    >
      <motion.div
        initial={false}
        animate={{
          scale: theme === "light" ? 1 : 0,
          rotate: theme === "light" ? 0 : 90,
          opacity: theme === "light" ? 1 : 0,
        }}
        transition={{ duration: 0.2 }}
        className="absolute inset-0 flex items-center justify-center"
      >
        <Sun className="h-5 w-5" />
      </motion.div>
      <motion.div
        initial={false}
        animate={{
          scale: theme === "dark" ? 1 : 0,
          rotate: theme === "dark" ? 0 : -90,
          opacity: theme === "dark" ? 1 : 0,
        }}
        transition={{ duration: 0.2 }}
        className="absolute inset-0 flex items-center justify-center"
      >
        <Moon className="h-5 w-5" />
      </motion.div>
    </Button>
  )
}
      </content>
    </args>
  </task>

  <task>
    <step>2</step>
    <description>Create the Template file for page transitions.</description>
    <tool>Write</tool>
    <args>
      <file_path>src/app/template.tsx</file_path>
      <content>
"use client"

import { motion } from "framer-motion"

export default function Template({ children }: { children: React.ReactNode }) {
  return (
    <motion.div
      initial={{ opacity: 0, y: 10 }}
      animate={{ opacity: 1, y: 0 }}
      exit={{ opacity: 0, y: -10 }}
      transition={{ ease: "easeInOut", duration: 0.4 }}
    >
      {children}
    </motion.div>
  )
}
      </content>
    </args>
  </task>

  <task>
    <step>3</step>
    <description>Add the Theme Toggle to the main layout or page header.</description>
    <tool>Edit</tool>
    <args>
      <file_path>src/app/page.tsx</file_path>
      <old_string>{profile ? `Welcome, ${profile.name}` : "Discover your numbers, define your destiny."}
          </p>
        </header></old_string>
      <new_string>{profile ? `Welcome, ${profile.name}` : "Discover your numbers, define your destiny."}
          </p>
        </header>

        <div className="absolute top-4 right-4 md:top-8 md:right-8">
            <ThemeToggle />
        </div></new_string>
    </args>
  </task>

  <task>
    <step>4</step>
    <description>Import ThemeToggle in page.tsx</description>
    <tool>Edit</tool>
    <args>
      <file_path>src/app/page.tsx</file_path>
      <old_string>import { Button } from "@/components/ui/button"</old_string>
      <new_string>import { Button } from "@/components/ui/button"
import { ThemeToggle } from "@/components/ui/theme-toggle"</new_string>
    </args>
  </task>
</tasks>
