---
phase: 03-immersive-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/numerology/lo-shu-grid.tsx
  - src/app/template.tsx
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Reduced-motion preferences respected in Lo Shu Grid"
    - "Reduced-motion preferences respected in Page Transitions"
  artifacts:
    - path: "src/components/numerology/lo-shu-grid.tsx"
      provides: "Accessibility-aware animation logic"
    - path: "src/app/template.tsx"
      provides: "Accessibility-aware transition logic"
  key_links: []
---

<objective>
Implement reduced motion support for immersive UI components to close accessibility gaps identified in verification.

Purpose: Ensure users with motion sensitivities can use the application without discomfort.
Output: Updated components that respect 'prefers-reduced-motion' media query.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepaknaik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/numerology/lo-shu-grid.tsx
@src/app/template.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reduced motion support to Lo Shu Grid</name>
  <files>src/components/numerology/lo-shu-grid.tsx</files>
  <action>
    Update LoShuGrid component to respect reduced motion preferences:
    1. Import `useReducedMotion` from "framer-motion"
    2. Define alternative variants for reduced motion (instant appearance or simple fade)
    3. Conditionally use simple variants when `useReducedMotion` returns true

    Current behavior: Staggered spring animation runs unconditionally.
    Desired behavior: When reduced motion is preferred, grid items appear immediately or with simple fade, no staggering.
  </action>
  <verify>
    Check that code implements `useReducedMotion` and conditionally applies variants.
    Since we cannot easily mock media queries in simple verification, ensure the logic exists.
  </verify>
  <done>
    LoShuGrid uses `useReducedMotion` to switch between animated and static/simple states.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add reduced motion support to Page Transitions</name>
  <files>src/app/template.tsx</files>
  <action>
    Update Template component to respect reduced motion preferences:
    1. Import `useReducedMotion` from "framer-motion"
    2. Modify motion props to disable Y-axis movement and transition duration when reduced motion is preferred

    Current behavior: Opacity fade + Y-axis slide runs unconditionally.
    Desired behavior: When reduced motion is preferred, use simple opacity fade or instant transition without movement.
  </action>
  <verify>
    Check that code implements `useReducedMotion` and conditionally applies transition props.
  </verify>
  <done>
    Page transitions (Template) respect reduced motion preferences.
  </done>
</task>

</tasks>

<verification>
Manual verification required:
1. Open browser dev tools -> Rendering -> Emulate CSS media feature prefers-reduced-motion: reduce
2. Navigate between pages -> verify no sliding/movement
3. Reload Lo Shu Grid -> verify no staggered spring animation
</verification>

<success_criteria>
- [ ] Lo Shu Grid animations are simplified/disabled when prefers-reduced-motion is true
- [ ] Page transitions are simplified/disabled when prefers-reduced-motion is true
</success_criteria>

<output>
After completion, create `.planning/phases/03-immersive-ui/03-02-SUMMARY.md`
</output>
