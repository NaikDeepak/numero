---
phase: 01-foundation-setup
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [
  "package.json",
  "pnpm-lock.yaml",
  "components.json",
  "src/components/ui/button.tsx",
  "src/components/theme-provider.tsx",
  "src/lib/utils.ts",
  "src/app/layout.tsx",
  "src/app/globals.css",
  "tailwind.config.ts"
]
autonomous: true

must_haves:
  truths:
    - "Developer can toggle between Light and Dark modes without page flash"
    - "shadcn/ui button component renders with proper styling"
    - "System theme preference is respected on first load"
    - "Theme persists across page refreshes"
  artifacts:
    - path: "components.json"
      provides: "shadcn/ui configuration"
      contains: "style"
    - path: "src/components/ui/button.tsx"
      provides: "shadcn button component"
      min_lines: 30
    - path: "src/components/theme-provider.tsx"
      provides: "next-themes wrapper component"
      contains: "ThemeProvider"
    - path: "src/lib/utils.ts"
      provides: "cn() utility for class merging"
      contains: "clsx"
    - path: "src/app/layout.tsx"
      provides: "Root layout with ThemeProvider"
      contains: "ThemeProvider"
    - path: "src/app/globals.css"
      provides: "CSS variables for light/dark themes"
      contains: "--background"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/theme-provider.tsx"
      via: "component wrapping"
      pattern: "ThemeProvider"
    - from: "src/components/theme-provider.tsx"
      to: "next-themes"
      via: "package import"
      pattern: "from \"next-themes\""
    - from: "src/components/ui/button.tsx"
      to: "src/lib/utils.ts"
      via: "cn utility import"
      pattern: "cn\\("
---

<objective>
Install and configure shadcn/ui component library with next-themes for dark mode support.

Purpose: Establish the UI foundation for Phase 3's immersive design. shadcn/ui provides accessible, customizable components that we'll animate in Phase 3. next-themes handles theme switching without hydration mismatches.

Output: Working theme system with first shadcn/ui component (button) installed and ready for use.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepaknaik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-setup/01-CONTEXT.md
@.planning/phases/01-foundation-setup/01-RESEARCH.md
@.planning/phases/01-foundation-setup/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure shadcn/ui</name>
  <files>
    package.json
    pnpm-lock.yaml
    components.json
    src/lib/utils.ts
    tailwind.config.ts
  </files>
  <action>
Initialize shadcn/ui with the "New York" style and Zinc base color (modern minimal aesthetic).

Steps:
1. Install shadcn/ui dependencies:
   ```bash
   cd /Users/deepaknaik/code/numero
   pnpm add class-variance-authority clsx tailwind-merge
   pnpm add -D tailwindcss-animate
   ```

2. Run shadcn/ui init (interactive CLI):
   ```bash
   pnpm dlx shadcn@latest init
   ```

   **CLI Prompts - Use these answers:**
   - Which style would you like to use? → **New York**
   - Which color would you like to use as base color? → **Zinc**
   - Do you want to use CSS variables for colors? → **Yes**
   - Where is your global CSS file? → **src/app/globals.css**
   - Would you like to use TypeScript? → **Yes**
   - Where is your tailwind.config located? → **tailwind.config.ts**
   - Configure the import alias for components: → **@/components**
   - Configure the import alias for utils: → **@/lib/utils**
   - Are you using React Server Components? → **Yes**

3. Verify components.json was created:
   ```bash
   cat components.json
   ```
   Should contain:
   ```json
   {
     "style": "new-york",
     "tailwind": {
       "baseColor": "zinc",
       "cssVariables": true
     }
   }
   ```

4. Verify src/lib/utils.ts was created with cn() utility:
   ```typescript
   import { clsx, type ClassValue } from "clsx"
   import { twMerge } from "tailwind-merge"

   export function cn(...inputs: ClassValue[]) {
     return twMerge(clsx(inputs))
   }
   ```

5. Verify Tailwind config was updated with shadcn variables:
   ```bash
   cat tailwind.config.ts | grep -A 5 "extend"
   # Should see color variables like --background, --foreground, etc.
   ```

6. Verify globals.css has CSS variables for light and dark themes:
   ```bash
   cat src/app/globals.css | grep -A 10 ":root"
   cat src/app/globals.css | grep -A 10 ".dark"
   ```

**Why New York style:** Modern, minimal aesthetic aligning with "Modern Minimal Design" decision.

**Why CSS variables:** Enables smooth theme transitions without regenerating styles.
  </action>
  <verify>
    Run these checks:
    1. `cat components.json | grep -q "new-york"` - Style configured
    2. `cat components.json | grep -q "zinc"` - Base color set
    3. `ls src/lib/utils.ts` - Utils file exists
    4. `cat src/lib/utils.ts | grep -q "twMerge"` - cn() utility defined
    5. `cat tailwind.config.ts | grep -q "border:"` - Color variables added
    6. `cat src/app/globals.css | grep -q "--background"` - CSS variables exist
  </verify>
  <done>
    - shadcn/ui initialized with New York style and Zinc base color
    - components.json configuration file created
    - src/lib/utils.ts created with cn() class merging utility
    - tailwind.config.ts extended with shadcn color variables
    - src/app/globals.css updated with :root and .dark theme variables
  </done>
</task>

<task type="auto">
  <name>Task 2: Install next-themes and create theme provider</name>
  <files>
    package.json
    pnpm-lock.yaml
    src/components/theme-provider.tsx
    src/app/layout.tsx
  </files>
  <action>
Install next-themes and create ThemeProvider component to wrap the app.

Steps:
1. Install next-themes:
   ```bash
   cd /Users/deepaknaik/code/numero
   pnpm add next-themes
   ```

2. Create theme provider component (src/components/theme-provider.tsx):
   ```typescript
   "use client"

   import * as React from "react"
   import { ThemeProvider as NextThemesProvider } from "next-themes"

   export function ThemeProvider({
     children,
     ...props
   }: React.ComponentProps<typeof NextThemesProvider>) {
     return <NextThemesProvider {...props}>{children}</NextThemesProvider>
   }
   ```

3. Update root layout (src/app/layout.tsx) to use ThemeProvider:
   ```typescript
   import type { Metadata } from "next"
   import { Inter } from "next/font/google"
   import "./globals.css"
   import { ThemeProvider } from "@/components/theme-provider"

   const inter = Inter({ subsets: ["latin"] })

   export const metadata: Metadata = {
     title: "Numero - Next Gen Numerology",
     description: "AI-driven numerological insights in a visually stunning experience",
   }

   export default function RootLayout({
     children,
   }: {
     children: React.ReactNode
   }) {
     return (
       <html lang="en" suppressHydrationWarning>
         <body className={inter.className}>
           <ThemeProvider
             attribute="class"
             defaultTheme="system"
             enableSystem
             disableTransitionOnChange
           >
             {children}
           </ThemeProvider>
         </body>
       </html>
     )
   }
   ```

   **Key props explained:**
   - `attribute="class"` - Toggles theme via class on <html> element (required for Tailwind dark mode)
   - `defaultTheme="system"` - Respects user's OS preference on first load
   - `enableSystem` - Allows system theme detection
   - `disableTransitionOnChange` - Prevents flash during initial theme load
   - `suppressHydrationWarning` - Prevents React warning due to server/client theme mismatch

4. Verify no hydration errors:
   ```bash
   pnpm dev
   # Check browser console for "Hydration failed" errors
   # Should be clean
   ```

**Why suppressHydrationWarning:** Server doesn't know user's theme preference (stored in localStorage), so <html> class differs between server and client render. This is expected and safe.

**Why disableTransitionOnChange:** Prevents jarring transition animations during initial page load theme detection.
  </action>
  <verify>
    Run these checks:
    1. `cat package.json | grep -q "next-themes"` - Package installed
    2. `cat src/components/theme-provider.tsx | grep -q "ThemeProvider"` - Component exists
    3. `cat src/app/layout.tsx | grep -q "ThemeProvider"` - Layout wraps app with provider
    4. `cat src/app/layout.tsx | grep -q "suppressHydrationWarning"` - Hydration warning suppressed
    5. Start dev server, check browser console - no hydration errors
  </verify>
  <done>
    - next-themes package installed
    - ThemeProvider wrapper component created
    - Root layout updated to wrap app with ThemeProvider
    - suppressHydrationWarning added to prevent React warnings
    - Theme system respects system preference on first load
  </done>
</task>

<task type="auto">
  <name>Task 3: Install shadcn button component and create theme toggle demo</name>
  <files>
    src/components/ui/button.tsx
    src/app/page.tsx
  </files>
  <action>
Install shadcn/ui button component and create a simple demo page with theme toggle to verify the foundation works.

Steps:
1. Install button component:
   ```bash
   cd /Users/deepaknaik/code/numero
   pnpm dlx shadcn@latest add button
   ```
   This creates src/components/ui/button.tsx automatically.

2. Verify button component was created:
   ```bash
   cat src/components/ui/button.tsx | head -20
   ```
   Should see Button component with variants (default, destructive, outline, etc.).

3. Create demo homepage (src/app/page.tsx) with theme toggle:
   ```typescript
   "use client"

   import { Button } from "@/components/ui/button"
   import { useTheme } from "next-themes"
   import { useEffect, useState } from "react"

   export default function Home() {
     const { theme, setTheme } = useTheme()
     const [mounted, setMounted] = useState(false)

     // Prevent hydration mismatch by only rendering theme-dependent UI after mount
     useEffect(() => {
       setMounted(true)
     }, [])

     if (!mounted) {
       return (
         <main className="flex min-h-screen flex-col items-center justify-center p-24">
           <div className="text-center">
             <h1 className="text-4xl font-bold mb-4">Numero</h1>
             <p className="text-lg text-muted-foreground">Next Gen Numerology</p>
           </div>
         </main>
       )
     }

     return (
       <main className="flex min-h-screen flex-col items-center justify-center p-24">
         <div className="text-center space-y-8">
           <div>
             <h1 className="text-4xl font-bold mb-4">Numero</h1>
             <p className="text-lg text-muted-foreground">
               Next Gen Numerology - Foundation Ready
             </p>
           </div>

           <div className="flex gap-4 justify-center">
             <Button
               variant="outline"
               onClick={() => setTheme("light")}
               className={theme === "light" ? "border-primary" : ""}
             >
               Light
             </Button>
             <Button
               variant="outline"
               onClick={() => setTheme("dark")}
               className={theme === "dark" ? "border-primary" : ""}
             >
               Dark
             </Button>
             <Button
               variant="outline"
               onClick={() => setTheme("system")}
               className={theme === "system" ? "border-primary" : ""}
             >
               System
             </Button>
           </div>

           <div className="text-sm text-muted-foreground">
             Current theme: {theme}
           </div>
         </div>
       </main>
     )
   }
   ```

4. Test theme switching:
   ```bash
   pnpm dev
   # Visit http://localhost:3000
   # Click Light/Dark/System buttons
   # Verify:
   # - Background color changes (white <-> dark)
   # - No page flash during transition
   # - Refresh page - theme persists
   # - Change OS theme - System mode responds
   ```

5. Verify CSS variables are working:
   - Open browser DevTools
   - Inspect <html> element
   - Toggle theme, watch class="dark" appear/disappear
   - Check computed styles, verify --background changes

**Why mounted check:** Prevents hydration mismatch by deferring theme-dependent rendering until client-side.

**Why "use client":** page.tsx uses useState/useEffect (client-only hooks). Theme toggle requires interactivity.

**IMPORTANT:** This demo page will be replaced in Phase 2 with actual numerology features. It's just for foundation verification.
  </action>
  <verify>
    Run these checks:
    1. `ls src/components/ui/button.tsx` - Button component exists
    2. `cat src/components/ui/button.tsx | grep -q "buttonVariants"` - Has variants
    3. `cat src/app/page.tsx | grep -q "useTheme"` - Demo page uses theme hook
    4. Start dev server, visit localhost:3000:
       - Click "Dark" button → background turns dark
       - Click "Light" button → background turns light
       - Refresh page → theme persists
       - No hydration errors in console
  </verify>
  <done>
    - shadcn/ui button component installed in src/components/ui/
    - Demo homepage created with theme toggle buttons
    - Theme switching works without page flash
    - Theme preference persists across page refreshes
    - System theme preference is respected
    - No hydration mismatch errors
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify the UI foundation is ready:

1. **Theme system works:**
   ```bash
   pnpm dev
   # Visit http://localhost:3000
   # Test all three theme modes (Light, Dark, System)
   # Verify smooth transitions, no flash
   ```

2. **shadcn/ui components work:**
   - Button renders with proper styling
   - Hover states work
   - Variants (outline) render correctly

3. **CSS variables update on theme change:**
   - Open DevTools → Elements
   - Inspect <html> element
   - Toggle theme, watch class="dark" toggle
   - Check Computed styles → --background value changes

4. **No hydration issues:**
   - Open DevTools → Console
   - Should be no "Hydration failed" errors
   - Should be no "Warning: Prop \`className\` did not match" warnings

5. **Theme persists:**
   - Set theme to Dark
   - Refresh page
   - Should still be Dark (localStorage working)

6. **System theme detection works:**
   - Set theme to "System"
   - Change OS theme preference (macOS: System Settings → Appearance)
   - Page should update automatically
</verification>

<success_criteria>
UI foundation is complete when:
- [ ] shadcn/ui initialized with New York style and Zinc base color
- [ ] components.json configuration exists
- [ ] cn() utility function works for class merging
- [ ] CSS variables for light/dark themes defined in globals.css
- [ ] next-themes package installed and configured
- [ ] ThemeProvider wraps app in root layout
- [ ] Button component renders with proper styling
- [ ] Theme toggle works without hydration errors
- [ ] Theme preference persists across page refreshes
- [ ] System theme preference is respected
- [ ] No hydration mismatch warnings in browser console
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-setup/01-02-SUMMARY.md` following the summary template.
</output>
