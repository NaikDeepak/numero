---
phase: 05-ai-reports
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: ["src/app/actions/compatibility.ts"]
autonomous: true

must_haves:
  truths:
    - "System calculates Moolank and Bhagyank for two distinct profiles"
    - "AI generates a Relationship Synergy report based on both sets of numbers"
    - "Compatibility results are cached to prevent redundant AI calls"
  artifacts:
    - path: "src/app/actions/compatibility.ts"
      provides: "AI Compatibility analysis logic"
  key_links:
    - from: "Compatibility Action"
      to: "Gemini API"
      via: "getGeminiClient"
---

<objective>
Enhance the compatibility analysis logic to provide deep AI-driven relationship insights.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepaknaik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/ai/prompts.ts
@src/lib/numerology/engine.ts
</context>

<tasks>

<task type="auto">
  <name>Implement AI Compatibility Action</name>
  <files>src/app/actions/compatibility.ts</files>
  <action>
    Enhance the `getCompatibility` Server Action.
    - Accept two profile inputs (User + Partner).
    - Calculate numerology metrics for both.
    - Construct a compatibility-specific prompt using `generateCompatibilityPrompt`.
    - Call Gemini to generate a "Synergy Overview", "Key Dynamics", and "Cosmic Advice".
    - Implement caching using a composite key of both profile IDs/DOBs.
    - Return structured data including both profiles' numbers and the AI analysis.
  </action>
  <verify>
    Run a test script or use a temporary endpoint to call the action with two DOBs and log the AI response.
  </verify>
  <done>
    Server action correctly calculates data for both profiles and returns AI-generated synergy insights.
  </done>
</task>

</tasks>

<verification>
Verify `src/app/actions/compatibility.ts` handles dual-profile calculations and AI integration.
</verification>

<success_criteria>
The backend can successfully analyze two profiles and provide a personalized relationship narrative.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-reports/05-03-SUMMARY.md`
</output>
