---
phase: 05-ai-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/lib/pdf/generator.ts"]
autonomous: true

must_haves:
  truths:
    - "A4 PDF documents can be generated server-side with branded headers"
    - "PDF includes sections for Moolank, Bhagyank, and the Lo Shu Grid"
  artifacts:
    - path: "src/lib/pdf/generator.ts"
      provides: "PDFKit generation utility"
  key_links:
    - from: "PDF Generator"
      to: "PDFKit"
      via: "Direct library calls"
---

<objective>
Implement the core PDF generation foundation using PDFKit to produce high-quality numerology reports.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepaknaik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/numerology/types.ts
</context>

<tasks>

<task type="auto">
  <name>Create PDF Generator Foundation</name>
  <files>src/lib/pdf/generator.ts</files>
  <action>
    Create a robust PDF generation utility using `pdfkit`.
    - Implement `generateNumerologyPDF(data, aiAnalysis)` function.
    - Setup A4 page layout with cosmic branding (colors, headers).
    - Implement helper functions for drawing the Lo Shu Grid and rendering interpretation text blocks.
    - Ensure text wrapping and pagination logic are handled for long AI reports.
  </action>
  <verify>
    Create a temporary script to run the generator and output a local `test.pdf`.
  </verify>
  <done>
    `src/lib/pdf/generator.ts` exists and can produce a valid PDF buffer/stream.
  </done>
</task>

</tasks>

<verification>
Check `src/lib/pdf/generator.ts` for export of generation function.
</verification>

<success_criteria>
The PDF generator is capable of taking numerology data and AI text to produce a styled PDF document.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-reports/05-01-SUMMARY.md`
</output>
