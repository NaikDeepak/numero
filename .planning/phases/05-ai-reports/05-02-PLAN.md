---
phase: 05-ai-reports
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: ["src/app/api/report/route.ts", "src/components/numerology/report-button.tsx"]
autonomous: false

must_haves:
  truths:
    - "User can click a download button on the home page"
    - "Clicking button triggers a PDF download with personalized AI report"
  artifacts:
    - path: "src/app/api/report/route.ts"
      provides: "PDF download endpoint"
    - path: "src/components/numerology/report-button.tsx"
      provides: "Download UI trigger"
  key_links:
    - from: "UI Button"
      to: "/api/report"
      via: "window.open or anchor tag"
---

<objective>
Expose the PDF generation via a Route Handler and add a download trigger to the User Interface.
</objective>

<execution_context>
@/Users/deepaknaik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepaknaik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/pdf/generator.ts
@src/app/api/report/route.ts
</context>

<tasks>

<task type="auto">
  <name>Implement PDF Route Handler</name>
  <files>src/app/api/report/route.ts</files>
  <action>
    Create a Next.js Route Handler (GET) to stream the PDF.
    - Parse `name` and `dob` from query parameters.
    - Calculate numerology data and fetch/generate AI report using `generateReportPrompt`.
    - Reuse `forecastCache` to avoid double AI calls.
    - Call `generateNumerologyPDF` from Plan 01.
    - Return a `NextResponse` with `Content-Type: application/pdf` and `Content-Disposition: attachment`.
    - Ensure `runtime = 'nodejs'` is set.
  </action>
  <verify>
    `curl -I "http://localhost:3000/api/report?name=Test&dob=1990-01-01"` returns 200 OK with PDF content-type.
  </verify>
  <done>
    Route handler successfully streams binary PDF data.
  </done>
</task>

<task type="auto">
  <name>Create Report Download UI</name>
  <files>src/components/numerology/report-button.tsx</files>
  <action>
    Create a `ReportButton` component using shadcn/ui.
    - Use `FileDown` icon from Lucide.
    - When clicked, trigger the `/api/report` download using the current user's profile data.
    - Integrate the button into the main dashboard view.
  </action>
  <verify>
    Check if button appears on Home page and triggers download.
  </verify>
  <done>
    Users can download their full report PDF with one click.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>PDF Download Integration</what-built>
  <how-to-verify>
    1. Open the application and ensure you have a profile saved.
    2. Click the 'Download Report' button.
    3. Verify that a PDF downloads and contains correct numerology data for your DOB.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Manual test of PDF download and content verification.
</verification>

<success_criteria>
The end-to-step flow from button click to PDF file in the user's downloads folder is functional.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-reports/05-02-SUMMARY.md`
</output>
